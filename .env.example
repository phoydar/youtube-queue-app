# YouTube
YOUTUBE_API_KEY=your-api-key-here

# Database (SQLite — relative to prisma/ directory)
DATABASE_URL="file:./dev.db"

# API Protection (generate with: openssl rand -hex 32)
API_SECRET=

# Vercel Cron (auto-injected in production)
CRON_SECRET=

# AI Integration (optional — without these, AI pipeline no-ops gracefully)
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
AI_SUMMARY_MODEL=claude-haiku-4-5-20251001
AI_EMBEDDING_MODEL=voyage-3-lite
